/**
 * The 40tag Guarantee — single source of truth for its wording.
 *
 * 40tag is not an authorized retailer for the brands it sells (All-Clad,
 * KitchenAid, Blackstone, etc.), so it cannot promise their manufacturer
 * warranties will be honored. Every warranty/guarantee claim on the site
 * therefore references this ONE store-backed guarantee instead — imported
 * everywhere it's shown (PDP, homepage, Terms) rather than re-typed, so it
 * cannot drift out of sync the way the old per-product `warrantyMonths`
 * field could. Owner decision 2026-08-12.
 */
export const GUARANTEE_MONTHS = 24
export const GUARANTEE_NAME = `40tag ${GUARANTEE_MONTHS}-Month Guarantee`
export const GUARANTEE_TAGLINE = 'We repair or replace, on us'
